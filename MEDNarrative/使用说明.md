# 叙事工具链 · 文案策划使用说明

> 面向叙事/文案策划的日常工作流指南。无需任何编程知识。
> 最后更新：2026-08-24（v1.3）

---

## 这套工具是什么

在 Obsidian 里完成 **流程设计（Flow）→ 对话编写（Dialogue）→ 导出游戏可用文件** 的全流程，导出的 `.dialogue` 文件可直接被 Godot 项目的 Dialogue Manager 读取。

由两个插件组成：

| 插件 | 作用 |
|------|------|
| **Narrative Canvas** | 对话编辑器：在画布上写对话、分支、选项（`.ncanvas` 文件） |
| **Narrative Tool** | 项目管理：角色/地点/物品/任务档案、Flow 画布、一键导出 |

### 概念结构（对标 articy:draft）

本工具链的流程层级照搬 articy:draft 的四层模型，两层由一种文件承担：

| articy:draft 概念 | 本工具链 | 文件形式 | 说明 |
|------------------|----------|---------|------|
| **Flow** | Flow | `.canvas`（如 `Flows/第一章.canvas`） | 最大的流程单元，如"第一章" |
| **Flow Fragment** | Flow 片段 | `.canvas`（如 `Flows/第一章/村口遭遇.canvas`） | Flow 下的子流程，必须挂在某个 Flow 的同名文件夹里 |
| **Dialogue** | 对话 | `.ncanvas` | 一段完整对话 |
| **Dialogue Fragment** | 对话片段 | `.ncanvas` 内部的节点结构 | 一段对话内部的小节（在 Narrative Canvas 里用节点和连线组织，可挂 Marker 标记） |

即：**`.canvas` 负责 Flow 和 Flow Fragment 两层（Obsidian 原生画布）；`.ncanvas` 负责 Dialogue 和 Dialogue Fragment 两层（Narrative Canvas 编辑器）**。

目录结构随之固定为：

```
Flows/
├── 第一章.canvas              ← Flow
└── 第一章/                    ← Flow 的同名文件夹
    ├── 村口遭遇.canvas         ← Flow Fragment
    └── …
Characters/  Locations/  Items/  Quests/   ← 实体档案（.md）
```

对话文件（`.ncanvas`）通过**文件列表右键 Flow/Fragment 画布 →「添加对话节点」**挂到流程的对应位置上。

---

## 安装与更新

1. 拿到 `output/` 文件夹下的两个目录：`narrative-canvas/` 和 `narrative-tool/`
2. 复制到你的 vault 的 `.obsidian/plugins/` 下（覆盖旧版即可）
3. Obsidian → 设置 → 第三方插件 → 开启 **Narrative Canvas** 和 **Narrative Tool**

> 以后每次更新都是同样操作：拿新的 `output/` 两个目录覆盖，重启插件。设置不会丢。

---

## 第一次使用：两个设置

打开 设置 → 第三方插件 → **Narrative Tool**：

- **导出路径**：导出的 `.dialogue` 文件放哪。点旁边的「浏览…」直接选 Godot 项目的对话目录（如 `D:/Godot项目/dialogues`）。留空则导出到 `.ncanvas` 源文件旁边
- **MED 扩展语法**：保持开启（除非程序侧明确说不用状态系统）
- **导出范围目录**：保持 `/`（整个库）

Narrative Canvas 的设置里有一项**共享角色目录**，默认 `Characters`，**不要改**（改了导出侧不跟随）。

---

## 正常工作流（一条龙）

### 第 1 步：建角色档案

`Ctrl+P` → **创建角色** → 输入 ID（英文小写，如 `wangyuchang`）和显示名（如 `王裕昌`）。

会在 `Characters/` 下生成一个角色档案 `.md`，可补充定位、语气、备注。

> **只要建这一次。** 之后所有对话文件都会自动认识这些角色——这就是共享角色表。改角色名也只改这个档案，所有地方自动跟随。

地点、物品、任务同理（`创建地点` / `创建物品` / `创建任务`）。

### 第 2 步：建 Flow（对标 articy 的 Flow 层）

`Ctrl+P` → **创建 Flow** → 输入名称（如 `第一章`）。

自动生成 `Flows/第一章.canvas`（Flow 画布）+ `Flows/第一章/` 同名文件夹（收纳它的片段）。

剧情量大时用 **创建 Flow 片段**（对标 articy 的 Flow Fragment 层）：选择父 Flow → 输入名称 → 片段建在父 Flow 的同名文件夹里，并自动在父 Flow 画布上生成引用节点。

### 第 3 步：在 Flow 上摆放内容

在文件列表**右键** Flow 画布文件：

- **添加对话节点** → 选一个 `.ncanvas` 对话文件挂上去（蓝色左边框）
- **添加角色/地点/物品/任务节点** → 把实体档案摆上画布（各有颜色区分）

### 第 4 步：写对话

双击画布上的对话节点（或直接新建/打开 `.ncanvas` 文件），进入 Narrative Canvas 编辑器：

- 从 **Entry（入口）** 节点开始，用连线连接各节点
- **Dialog（对话）** 节点写台词。说话人直接打字，**输入前几个字就会有角色名自动补全**（来自第 1 步的角色档案）
- 角色页里带「共享」徽标的角色来自角色档案，是只读的——要改请去改 `Characters/` 里的档案文件
- **Choice（选择）** 节点写分支选项，每个选项拉线到各自的后续
- **Marker（标记）** 节点用于标记跳转点

保存后**自动导出** `.dialogue` 到设置的导出路径（状态栏会显示结果）。

### 第 5 步：检查与交付

- 状态栏显示 `✓ 已导出` 即成功；显示 `✗` 会把具体错误写出来（比如 JSON 格式错误、哪个文件失败）
- `Ctrl+P` → **批量导出所有对话**：一次性重导出全部对话（状态栏显示 `导出中 x/n` 进度）
- `Ctrl+P` → **验证 Flow→对话引用**：检查 Flow 画布上挂的对话文件有没有被移动/删除导致断链
- 在 `.ncanvas` 文件上右键 → **打开 Flow 画布**：从对话跳回它所属的流程图

---

## 全部指令一览

### 命令面板指令（`Ctrl+P` 搜索，共 10 个）

| 指令 | 功能 |
|------|------|
| **创建角色** | 在 `Characters/` 下创建角色档案（输入 ID + 显示名） |
| **创建地点** | 在 `Locations/` 下创建地点档案 |
| **创建物品** | 在 `Items/` 下创建物品档案 |
| **创建任务** | 在 `Quests/` 下创建任务档案 |
| **创建 Flow** | 输入名称，创建 `Flows/<名称>.canvas` + 同名文件夹 |
| **创建 Flow 片段** | 选择父 Flow 后输入名称，片段建在父 Flow 同名文件夹并自动挂回父画布 |
| **导出当前对话** | 把当前打开的（或选择的）`.ncanvas` 导出为 `.dialogue` |
| **批量导出所有对话** | 导出范围内全部 `.ncanvas`，状态栏显示 `导出中 x/n` 进度 |
| **验证 Flow→对话引用** | 检查所有 `.canvas` 里挂载的对话文件是否断链 |
| **打开 Flow 画布** | 从当前 `.ncanvas` 反向跳回引用它的 Flow 画布 |

### 文件列表右键菜单

**右键 `.canvas` 文件（Flow / Flow Fragment）：**

| 菜单项 | 功能 |
|--------|------|
| **添加对话节点** | 选择 `.ncanvas` 文件挂到画布上（蓝色左边框） |
| **添加角色节点** | 从 `Characters/` 选角色摆上画布（绿色） |
| **添加地点节点** | 从 `Locations/` 选地点摆上画布（橙色） |
| **添加物品节点** | 从 `Items/` 选物品摆上画布（红色） |
| **添加任务节点** | 从 `Quests/` 选任务摆上画布（紫色） |
| **打开关联对话** | 打开此画布引用的对话文件（多个时弹出选择） |

**右键 `.ncanvas` 文件（对话）：**

| 菜单项 | 功能 |
|--------|------|
| **打开 Flow 画布** | 反向跳回引用该对话的 Flow 画布 |

---

## 对话编写要点（影响导出结果）

### 说话人

- 台词写成 `王裕昌: "……"` 或直接靠节点的角色标注，导出都会正确生成 `王裕昌: "……"`，**不会重复加名字**
- 没有说话人的节点导出为旁白（纯叙述文本）

### 分支选项（Choice）

每个选项缩进一级，导出为：

```
- 选项一
    后续对话……
- 选项二
    后续对话……
```

### 循环返回（"问完了还回来"）

常见结构：几个"打听消息"选项问完要回到选择，一个"正事"选项推进剧情。做法：**把选项的后续节点连线画回 Choice 节点本身**。导出会自动生成跳转标记，玩家可以反复选择。

### 路径汇合（多个选项殊途同归）

多个分支后面接同一段对话时，直接把线都连到同一个节点即可——共享内容只会导出一次，各分支自动跳转汇合。如果汇合点你放了 Marker 节点，导出的跳转名会沿用 Marker 的名字。

### 按状态自动分支（根据 flag 走向，玩家看不到选项）

**需求场景**：A 对话里设置了某个 flag（如 `set_flag 123`），B 对话要根据这个 flag 自动走不同分支——不是给玩家选的 Choice，而是条件成立走一条路、不成立走另一条。

**条件写在哪：写在 B 对话节点的出口连线上，不是节点的"状态逻辑"里。** 节点检查器里"状态逻辑"下的两个字段都不是用来干这个的：

| 字段 | 实际作用 | 为什么不能写这 |
|------|---------|---------------|
| 状态逻辑 → 条件（条件表达式） | 判断"这个节点能不能进入"，只在编辑器内的「演示」预览生效 | **导出 `.dialogue` 时会被忽略**，条件丢失 |
| 状态逻辑 → 效果（效果语句） | "改状态"的地方（如 `onVisit set_flag flag_123 true`），是**写** flag | 不是**判** flag |

#### 填写方法

在画布上直接操作连线，无需手写任何文本：

1. 在画布上**左键单击** B 对话节点引出的那条连线（或右键连线选「编辑条件」），右侧检查器会切换到连线面板
2. 在「条件」区用状态下拉 + 运算符 + 值拼条件，点「添加条件」追加子句；也可以直接在条件表达式文本框里手写整段表达式，面板下方会实时显示条件在当前变量下的判定状态
3. 条件留空 = else 分支（同一节点其他带条件的出线都不满足时走这条）；多条带条件的出线按顺序判定。带条件的连线在画布中点会显示 `if …` 标记
4. 保存后自动导出的 `.dialogue` 会生成 `[if]…[else]…[/if]` 分支块

#### 案例

A 对话里通过选项效果设置 flag（选项 → 效果 → `onChoose set_flag flag_123 true`）。

B 对话拉两条出线：一条到「已见过」节点，在连线面板填 `flag_123 === true`；另一条到「初次见」节点，条件留空（即 else 分支）。

导出结果：

```
王裕昌: "你来了。"
[if flag_123 === true]
	……（"已见过"分支内容）
[else]
	……（"初次见"分支内容）
[/if]
```

只有一条分支、条件不满足就不走（后面没有内容）的情况，只拉一条带条件的出线即可——导出会生成不带 `[else]` 的 `[if]…[/if]` 块。

#### 条件表达式可用写法

- 单独一个 flag 名：`flag_123`（真值判定），取反：`!flag_123`
- 比较：`flag_123 === true`、`res_coins >= 5`、`!==`、`<=`、`>`
- 组合：`flag_a && flag_b`、`flag_a || flag_b`、`A && (B || C)`
- 包含：`flag_tokens.includes("boarding")`

> 条件里引用的变量名要和程序侧实际存储的名字一致（MED 导出 `do set_flag` 时对 `flag_`/`res_` 前缀的处理约定，问程序侧）。同一节点多条带条件的出线按顺序判定，生成 `[if]…[else][if]…[/if]…[/if]` 嵌套块；无条件的那条线就是 `[else]` 分支。

### MED 状态语法（需要程序侧配合的功能）

在文本里直接写：`do set_flag(...)`、`[#check=...]`、`[term=...]` 等，会原样导出。具体可用哪些问程序侧。

---

## 常见问题

**Q：新建对话文件后角色是空的？**
不会了。只要 `Characters/` 里有角色档案，打开任何对话文件都能直接用（角色页带「共享」徽标）。如果没有，检查角色档案是否在 `Characters/` 目录下、文件开头 `---` 之间的属性区（id、name 等）有没有被改坏。

**Q：改了角色档案的名字，对话里没更新？**
会自动更新（约半秒延迟）。如果没有，切换一下标签页或重开文件。

**Q：导出失败怎么办？**
看状态栏的错误信息。最常见是 `.ncanvas` 文件被手动改坏了（JSON 格式错误）——用 Narrative Canvas 重新打开保存一次通常能修复。

**Q：导出的文件在哪里？**
看设置里的「导出路径」。批量导出时如果两个对话文件同名，后导出的会自动加上父文件夹名前缀，不会互相覆盖。

**Q：想删掉某个共享角色？**
删除或移出 `Characters/` 里对应的 `.md` 档案即可，对话编辑器里会自动消失。注意已写好对话里的名字文本不受影响（那是你写进正文的内容）。

---

*技术细节见 README.md；导出的 `.dialogue` 语法契约由程序侧与本工具链共同维护。*
